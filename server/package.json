{
  "name": "gradkart-server",
  "version": "1.0.0",
  "description": "Express backend for GradKart marketplace",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "echo 'No build step for backend'",
    "heroku-postbuild": "cd ../client && npm install && npm run build",
    "postinstall": "cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["express", "railway", "deployment"],
  "author": "Your Name",
  "license": "MIT"
} 